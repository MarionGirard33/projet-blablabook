# projet-blablabook

## Other

### Swagger

BlablaBook is documented with Swagger on the endpoint `http://localhost:3000/api`

---

### Adminer

For show DB with adminer, use this endpoint `http://localhost:8080` and this credentials :

- Host: `postgres` (name service in docker compose)
- Username: `postgres`
- Pwd: `postgres`
- Database name: `blablabook`
